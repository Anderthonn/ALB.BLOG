@model List<ALB.BLOG.BLO.ViewModels.UserVM>

@{
    ViewData["Title"] = "Home";
}

<div class="container-fluid">
    <a class="btn mb-1 btn-alb-page" asp-action="Register" asp-controller="User"><i class="bi bi-plus-circle-fill"></i> Adicionar Usuárior</a>
    <div class="card">
        <div class="card-header" style="background-color: #00B8E4;">
            <h4 class="card-title text-white">Lista de Usuários</h4>
        </div>
        <div class="card-body">
            <table class="table table-striped table-hover" id="myTable">
                <thead>
                    <tr>
                        <th>Primeiro Nome</th>
                        <th>Sobrenome</th>
                        <th>Nome de Usuário</th>
                        <th>Email</th>
                        <th>Papel</th>
                        <th>Ação</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach(var user in Model)
                    {
                        <tr>
                            <td>@user.FirstName</td>
                            <td>@user.LastName</td>
                            <td>@user.UserName</td>
                            <td>@user.Email</td>
                            <td>@user.Role</td>
                            <td>
                                <a class="btn btn-info" asp-action="ResetPassword" asp-controller="User" asp-route-id="@user.Id">Redefinir Senha</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>